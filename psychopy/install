#!/usr/bin/env bash
# Create a new python environment on a Mac to run psychopy experiments
# Requirements:
# - anaconda (brew cask install anaconda)

# Create a lab environment
source ~/anaconda3/bin/activate
conda env create environment.yml
source activate lab

# erik/psychopy installs a version of pyglet that doesn't work
# so install one that does:
conda install --force -c jcarlin pyglet
